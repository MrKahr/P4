    IF(attacked EQUALS 3) {
        IF(activePlayer EQUALS p1) {
            AttackPlayer(attackingMinion, playerOne);
        } ELSE {
            AttackPlayer(attackingMinion, playerTwo);
        }
    } ELSE { 
        IF(nacctivePlayer EQUALS p1) {
            Card attackedMinion IS 9;
        } ELSE {
            Card attackedMinion IS board;
        }
    }

    IF(e > f ){}