/* TEST 1
Action Decl with parameters, without return
*/
Action rad(Integer a, Integer b) RESULTS IN Integer {
    RESULT IN a+b;
}


Rule r1 WHEN [rad] IF(true){
    Integer i IS rad.RESULT;
    i IS i + 5; // This should equal 8
}

Integer v1 IS rad(1,2);